<UserControl
    x:Class="FinancingApp.View.TransactionCreation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FinancingApp.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uiLogic="clr-namespace:FinancingApp.UiLogic"
    xmlns:vm="clr-namespace:FinancingApp.ViewModel"
    d:Background="AliceBlue"
    d:DataContext="{d:DesignInstance vm:TransactionCreationViewModel}"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding AddTransactionCommand}" />
    </UserControl.InputBindings>
    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBox">
                <Setter Property="uiLogic:FocusBehavior.SelectTextOnFocus" Value="True" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MaxWidth="50" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" MaxWidth="50" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label
            Grid.Row="0"
            Grid.Column="1"
            HorizontalAlignment="Center"
            FontWeight="Bold">
            Füge eine neue Transaktion hinzu:
        </Label>
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" MaxWidth="80" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <!--  date picker  -->
            <local:DatePicker
                Grid.Column="0"
                HorizontalAlignment="Center"
                Date="{Binding Date}" />
            <!--  select category  -->
            <ComboBox
                Grid.Column="2"
                Width="120"
                IsEditable="True"
                ItemsSource="{Binding AvailableCategories}"
                SelectedItem="{Binding SelectedCategory}"
                TextSearch.TextPath="Name">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <!--  description  -->
            <TextBox Grid.Column="4" Text="{Binding Description}" />
            <!--  amount  -->
            <TextBox Grid.Column="6" Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}">
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="LostFocus">
                        <b:InvokeCommandAction Command="{Binding ConvertToCurrencyCommand}" />
                    </b:EventTrigger>
                </b:Interaction.Triggers>
                <TextBox.Style>
                    <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsAmountValid}" Value="False">
                                <Setter Property="BorderBrush" Value="Red" />
                                <Setter Property="BorderThickness" Value="2" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
            <Button
                Grid.Column="8"
                MinWidth="30"
                Command="{Binding AddTransactionCommand}">
                <Button.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding AddTransactionCommand}" />
                </Button.InputBindings>
                +
            </Button>
        </Grid>
    </Grid>
</UserControl>
